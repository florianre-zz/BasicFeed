<h1>Basic Feed</h1>
<%= bootstrap_form_for @post do |f| %>
  <%= f.text_area :description, hide_label: true, placeholder: "Leave a comment..." %>
  <%= f.submit "Post" %>
<% end %>

<% Post.all.each do |post| %>
  <div>
    <%= post.description %>
  </div>
<% end %>
